# Optional. Without this variable, the app automatically uses browser localStorage.
# Recommended on Vercel: provision a PostgreSQL database from the Vercel Marketplace.
DATABASE_URL="postgresql://USER:PASSWORD@HOST:5432/DATABASE?sslmode=require"
